# Rescura Sync Backend

FastAPI backend for Rescura Sync.

Run with Docker Compose:

- `docker compose up --build`

API endpoints:

- `GET /api/resources/`
- `POST /api/resources/`
- `GET /api/resources/{id}`
